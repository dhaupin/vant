title: Vant
description: AI memory for agents
markdown: kramdown

# Build settings
permalink: pretty
exclude: ['README.md', 'Gemfile', 'Gemfile.lock']

# Default layouts for all docs
defaults:
  - scope:
      type: page
    values:
      layout: default
